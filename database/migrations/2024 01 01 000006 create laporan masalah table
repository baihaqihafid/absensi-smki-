<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('laporan_masalah', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('user_id');
            $table->string('judul');
            $table->text('deskripsi');
            $table->enum('kategori', ['Bug/Error', 'Tampilan', 'Fitur', 'Lainnya'])->default('Bug/Error');
            $table->enum('status', ['Belum Ditangani', 'Sedang Diproses', 'Selesai'])->default('Belum Ditangani');
            $table->text('balasan')->nullable();
            $table->timestamp('created_at')->useCurrent();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('laporan_masalah');
    }
};